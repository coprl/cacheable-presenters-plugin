source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in cacheable_presenter_plugin.gemspec
gemspec

gem 'coprl', github:'rx/presenters', branch: :beta

group :test do
  gem 'rspec'
end
